// #include "cub3d.h"

// int	cast_ray(t_app *app, double ray_dir_x, double ray_dir_y)
// {
// 	double	hit_x;
// 	double	hit_y;

// 	if (perform_dda(app, ray_dir_x, ray_dir_y, &hit_x, &hit_y))
// 		return (1);
// 	if (draw_ray(app, app->player.x * MINIMAP_TILE, app->player.y * MINIMAP_TILE,
// 			hit_x * MINIMAP_TILE, hit_y * MINIMAP_TILE, 0xFF0000))
// 		return (1);
// 	return (0);
// }
